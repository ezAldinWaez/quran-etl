# `data/manzil/`

7 paired files: `01.json` … `07.json` and `01.min.json` … `07.min.json`, plus paired indexes.

Each manzil is a weekly recitation partition with every ayah embedded under `ayahs` in the full edition or `a` in the minified edition.
