# `data/ruku/`

556 paired files: `001.json` … `556.json` and `001.min.json` … `556.min.json`, plus paired indexes.

Each ruku is a thematic range contained within one surah and embeds every ayah under `ayahs` in the full edition or `a` in the minified edition.
