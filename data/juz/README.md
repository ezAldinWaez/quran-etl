# `data/juz/`

30 paired files: `01.json` … `30.json` and `01.min.json` … `30.min.json`, plus paired indexes.

Each juz is a contiguous recitation partition with every ayah embedded under `ayahs` in the full edition or `a` in the minified edition.
