# `data/rub/`

240 paired files: `001.json` … `240.json` and `001.min.json` … `240.min.json`, plus paired indexes.

Each rub is one quarter of a hizb and embeds every ayah under `ayahs` in the full edition or `a` in the minified edition.
