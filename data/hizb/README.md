# `data/hizb/`

60 paired files: `01.json` … `60.json` and `01.min.json` … `60.min.json`, plus paired indexes.

Each hizb is half a juz and embeds every ayah under `ayahs` in the full edition or `a` in the minified edition. Hizb boundaries are derived from the 240 Tanzil quarter boundaries.
