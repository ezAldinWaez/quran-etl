# `data/page/`

604 paired files: `001.json` … `604.json` and `001.min.json` … `604.min.json`, plus paired indexes.

Each file represents one Medina Mushaf page and embeds every ayah under `ayahs` in the full edition or `a` in the minified edition.
